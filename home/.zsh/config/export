export CLICOLOR=1
export LSCOLORS=cxfxexexDxexexDxDxcxcx
export AUTOFEATURE=true #Test Cucumber
export LESS="-mR" #Line-numbers and ASCII-Colors in less
export EDITOR='mvim -f --nomru -c "au VimLeave * !open -a iTerm"'
export LC_ALL="en_US.UTF-8"
export MAVEN_OPTS='-Xms512m -Xmx1024m -XX:MaxPermSize=768m -XX:+CMSClassUnloadingEnabled -Dfile.encoding=UTF-8'
export DEBUG=1

# Show `time` output for any command that takes longer than 1 second of CPU time
export REPORTTIME=1

# Manpager with ascii-sequences
export MANPAGER=more

export PATH=/usr/local/bin:/usr/local/sbin:/usr/local/share/npm/bin/jshint:$PATH

#orcale instant client
export OCI_DIR=/opt/oracle/instantclient_11_2
export NLS_LANG="AMERICAN_AMERICA.UTF8"  
#export SQLPATH="/usr/local/oracle/instantclient_11_2"  
#export TNS_ADMIN="/usr/local/oracle/network/admin"  

export CMS_CONTENT_PATH=/Users/andrejahrling/repositories/svn/cmsContent/

# NVM (Node)
export NVM_DIR="$HOME/.nvm"
. "$(brew --prefix nvm)/nvm.sh"

# Homebrew
export HOMEBREW_INSTALL_BADGE="🛠"

# autojump
export PATH=/usr/local/bin:$PATH
[[ -s $(brew --prefix)/etc/profile.d/autojump.sh ]] && . $(brew --prefix)/etc/profile.d/autojump.sh

#rbenv
export PATH="$HOME/.rbenv/bin:$PATH"
if which rbenv > /dev/null; then eval "$(rbenv init - zsh)"; fi
